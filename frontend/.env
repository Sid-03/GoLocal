# React environment variables MUST start with REACT_APP_
# This URL should point to your API Gateway, accessible from the user's browser
REACT_APP_API_GATEWAY_URL=http://localhost:9000/api

# Optional: Disable fast refresh in development if causing issues (rare)
# FAST_REFRESH=false

# Optional: Set browser launch behavior for `npm start`
# BROWSER=none